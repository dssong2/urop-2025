# urop-2025
Go to model>my_method.ipynb and run all

Notes:
- Run pip install "requirements.txt" to install all dependencies in terminal
- Follow instructions to get US Census API Key (required)
- Follow instructions to get Google Maps API Key if you want to create the Arc graph (not required)
- Make sure to update path to any files by updating the corresponding variable to the the copied path of the file location
